# Douyin Knowledge Ingest 产品化与知识库连接规划

> 文档性质：方向规划。仓库现状更新于 2026-09-21（仓库文件与测试人工核对，强）；产品判断与优先级属于规划建议（推断，未证实），仍应在真实用户和公开演示中验证。

## 1. 项目定位

不要只把本项目描述成“抖音转文本”。建议对外定位为：

> 把短视频、图文和收藏内容转化成可追溯、可审核的本地知识库：完成 ASR/OCR、来源保留、主题组织和人工确认。

当前仓库已经包含视频转录、图文/画面 OCR、视觉资产、来源包、索引、审核状态、主题整理和 MCP 接口等基础能力（仓库文件人工核对，强）。真正需要加强的主要是公开叙事、低门槛演示、可复现评测和跨项目接口（推断，未证实）。

## 2. 产品化落实顺序

### P0：做出可公开复现的最小闭环

- ✅ 已加入可公开分发的 25 秒公共领域真人视频、原始片头帧和人工文字；许可证与哈希可核验。
- ✅ 已提供 `public_media_demo.py`，不依赖个人抖音登录状态。
- ✅ 演示会保存来源信息、视频转录时间戳来源包、OCR、审阅状态、清单和人工确认后的知识笔记。
- ✅ 已生成 66 秒 1080p 公开演示视频与 12 秒 README 预览，包含公共领域真人素材、真实运行录屏、真人原声、中文旁白和字幕，并覆盖提取、来源包、审阅和确认知识笔记。
- ✅ README 已说明问题、快速开始、本地数据边界，并展示公开图片夹具与真实 ASR/OCR 输出。

完成标准：陌生用户能在干净环境中使用示例素材走通一次，不需要接触维护者的 Cookie 或私人收藏。

### P1：稳定模块边界

- 为来源适配器定义统一接口：抖音、Bilibili、本地文件以后都经过同一来源对象。
- 为 ASR、OCR、视觉抽帧和导出器定义清晰接口，避免把具体实现写死在主流程。
- 统一作品状态：待处理、已提取、待审核、已确认、已拒绝、需重试。
- 保持来源包不可变；整理结果通过版本和来源引用关联，不覆盖原始证据。

完成标准：替换一种 ASR/OCR 实现或增加一种来源时，不需要重写整个入库流程。

### P2：建立证据优先的质量基准

- 建立至少一组固定混合素材夹具。
- 分别评测 ASR 覆盖、OCR 完整度、媒体类型识别、去重、来源链接、失败恢复。
- 对“信息是否足以进入知识库”保留人工审核，不把语言流畅度当作事实正确率。
- 每次修改抽取或整理逻辑后运行回归测试并保留结果。

完成标准：README 中的质量结论都能指向公开夹具、测试命令和真实结果。

### P3：公开发布与持续维护

- 增加版本发布说明、变更日志、Issue 模板和贡献入口。
- 明确各平台适配器的稳定性边界以及登录态风险。
- 不提交 Cookie、个人收藏、私人转录、模型缓存和受版权限制的示例内容。
- 使用真实演示和失败案例宣传，不承诺对平台变化永远稳定。

## 3. 与个人知识库的连接

知识库不应该直接读取任意中间文件，而应通过稳定的“知识候选包”连接本项目。

建议每个候选包至少包含：

- `source_id`、原始链接、平台、作者、发布时间和采集时间；
- 内容类型、转录/OCR 正文和时间戳/页序；
- 主题标签、摘要、待讨论问题；
- 引用到原视频时间点、图像或来源包的证据定位；
- 状态、版本、生成工具版本和人工确认记录。

建议的数据流：

```text
原始短视频/图文
    -> 本项目提取并保存来源包
    -> 生成“待讨论”的知识候选包
    -> ViDRAG 检索、比较和检查来源关系
    -> 人工讨论与确认
    -> 个人知识库正式笔记
```

重要边界：本项目负责“忠实提取和保留出处”，ViDRAG 负责“检索、主张与证据关系”，个人知识库负责“已经确认、值得长期保留的结论”（规划建议，推断，未证实）。

## 4. 为未来短视频创作者 Agent 预留的接口

未来 Agent 可以读取知识库中已经确认的结论，也可以读取本项目保存的原始证据，但不能把未经确认的自动摘要当成宣传事实。

建议输出给 Creator Agent 的对象包含：

- 已批准的事实或主张；
- 对应来源和可用于画面的时间段；
- 可公开使用状态及版权备注；
- 项目演示素材、失败案例和真实评测数据；
- 禁止宣传或仍待确认的内容。

Creator Agent 首期只生成发布包和预览，不直接自动上传；发布前保留事实、版权和成片人工确认（规划建议，推断，未证实）。

## 5. 高关注度不是第一验收指标

无法保证 GitHub 星数。当前更合理的领先指标是：

- 新用户首次跑通时间；
- 示例闭环成功率；
- 来源可追溯率；
- 人工从素材整理成合格笔记所需时间；
- 公开演示带来的真实安装、Issue、复用和贡献。

不要为了技术栈展示提前加入 Promptfoo、Docling、Langfuse 或 Context7。只有现有评测、文档解析、线上追踪或版本文档获取出现明确缺口时再引入（规划建议，推断，未证实）。

## 6. 下次继续时的入口

优先从 P0 开始逐项讨论：

1. 选什么公开示例素材；
2. 一条命令应输出哪些文件；
3. 60～90 秒演示展示哪些画面；
4. README 首屏怎样重写；
5. 哪些指标能诚实证明项目价值。
